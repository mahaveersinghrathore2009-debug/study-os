# Shared

Type contracts shared between frontend and backend.

## What lives here

- `types.ts` — common TypeScript types (sessions, subjects, goals, analytics,
  AI responses) mirrored from the backend Pydantic schemas in `backend/app/schemas.py`
